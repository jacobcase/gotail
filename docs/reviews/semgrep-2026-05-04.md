# Semgrep findings — gotail

- **Conducted:** 2026-05-04
- **Tool:** Semgrep OSS 1.161.0
- **Mode:** Run all (no severity filter)
- **Engine:** OSS
- **Source SARIF:** `docs/reviews/semgrep-2026-05-04/results/results.sarif`
- **Rulesets:** p/golang, p/security-audit, p/owasp-top-ten, p/secrets, trailofbits/semgrep-rules (go/), dgryski/semgrep-go
- **Total findings:** 7 (deduplicated by rule + location + snippet)

## Severity histogram

| Severity | Count |
| --- | --- |
| HIGH | 4 |
| MEDIUM | 3 |

## Findings by rule

| Rule | Count | Severity |
| --- | --- | --- |
| `dgryski.os-error-is-not-exist` | 3 | HIGH |
| `trailofbits.go.unsafe-dll-loading` | 1 | HIGH |
| `go.lang.security.audit.crypto.math_random.math-random-used` | 1 | MEDIUM |
| `go.lang.security.audit.unsafe.use-of-unsafe-block` | 2 | MEDIUM |

## Findings (sorted by severity, then rule, then file)

### 1. [HIGH] `dgryski.os-error-is-not-exist`

- **Location:** `tail/cursor.go:234`
- **Precision:** very-high

**Message:** New code should use errors.Is with the appropriate error type

```go
	if os.IsNotExist(err) {
```

### 2. [HIGH] `dgryski.os-error-is-not-exist`

- **Location:** `v1/poll_watcher.go:84`
- **Precision:** very-high

**Message:** New code should use errors.Is with the appropriate error type

```go
			if os.IsNotExist(err) {
```

### 3. [HIGH] `dgryski.os-error-is-not-exist`

- **Location:** `v1/poll_watcher.go:122`
- **Precision:** very-high

**Message:** New code should use errors.Is with the appropriate error type

```go
		} else if os.IsNotExist(err) {
```

### 4. [HIGH] `trailofbits.go.unsafe-dll-loading`

- **Location:** `tail/flock_windows.go:18`
- **Precision:** very-high
- **Tags:** CWE-427: Uncontrolled Search Path Element, HIGH CONFIDENCE, security

**Message:** The function is vulnerable to DLL hijacking attacks. Use `windows.NewLazySystemDLL()` function to limit DLL search to the Windows directory

```go
	modkernel32      = syscall.NewLazyDLL("kernel32.dll")
```

### 5. [MEDIUM] `go.lang.security.audit.crypto.math_random.math-random-used`

- **Location:** `forward/forward.go:8`
- **Precision:** very-high
- **Tags:** CWE-338: Use of Cryptographically Weak Pseudo-Random Number Generator (PRNG), MEDIUM CONFIDENCE, OWASP-A02:2021 - Cryptographic Failures, OWASP-A04:2025 - Cryptographic Failures, security
- **Help:** https://semgrep.dev/r/go.lang.security.audit.crypto.math_random.math-random-used

**Message:** Do not use `math/rand`. Use `crypto/rand` instead.

```go
	"math/rand/v2"
```

### 6. [MEDIUM] `go.lang.security.audit.unsafe.use-of-unsafe-block`

- **Location:** `tail/flock_windows.go:43`
- **Precision:** very-high
- **Tags:** CWE-242: Use of Inherently Dangerous Function, LOW CONFIDENCE, security
- **Help:** https://semgrep.dev/r/go.lang.security.audit.unsafe.use-of-unsafe-block

**Message:** Using the unsafe package in Go gives you low-level memory management and many of the strengths of the C language, but also steps around the type safety of Go and can lead to buffer overflows and possible arbitrary code execution by an attacker. Only use this package if you absolutely know what you're doing.

```go
		uintptr(unsafe.Pointer(&ol)),
```

### 7. [MEDIUM] `go.lang.security.audit.unsafe.use-of-unsafe-block`

- **Location:** `tail/flock_windows.go:66`
- **Precision:** very-high
- **Tags:** CWE-242: Use of Inherently Dangerous Function, LOW CONFIDENCE, security
- **Help:** https://semgrep.dev/r/go.lang.security.audit.unsafe.use-of-unsafe-block

**Message:** Using the unsafe package in Go gives you low-level memory management and many of the strengths of the C language, but also steps around the type safety of Go and can lead to buffer overflows and possible arbitrary code execution by an attacker. Only use this package if you absolutely know what you're doing.

```go
	procUnlockFileEx.Call(uintptr(h), 0, 1, 0, uintptr(unsafe.Pointer(&ol))) //nolint:errcheck
```

---

## Notes

- `level` on each SARIF result was `null` in the merged file; severity was joined from each rule's `defaultConfiguration.level` (`error` -> HIGH, `warning` -> MEDIUM, `note` -> LOW).
- Rule IDs from locally-cloned third-party rulesets had a `docs.reviews.semgrep-2026-05-04.repos.<repo>.` prefix injected by Semgrep (the path used as `--config`). The summary strips it for readability.
- Triage these for true vs. false positive — Semgrep's pattern matching is syntactic, so use of `unsafe`, `math/rand`, etc. is flagged regardless of context.
